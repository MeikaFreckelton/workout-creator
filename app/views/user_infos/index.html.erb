<p id="notice"><%= notice %></p>

<h1>Your Profile</h1>

<div class="profile">
  <h5>(insert profile image and profile image upload feature)</h5>

  <h5><%#=@current_user_info %></h5>
  <%#= @current_user_info%>
  <h5>D.O.B.</h5>
  <h5>Gender</h5>
  <h5>Weight</h5>
  <h5>Height</h5>

</div>




<table>
  <thead class="show-users-head">
    <tr>
      <th>Name</th>
      <th>Date of birth</th>
      <th>Gender</th>
      <th>Weight</th>
      <th>Height</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody class="show-users-body">
    <% @user_infos.each do |user_info| %>
      <tr class="users-table">
        <td><%= user_info.name %></td>
        <td><%= user_info.date_of_birth %></td>
        <td><%= user_info.gender %></td>
        <td><%= user_info.weight %></td>
        <td><%= user_info.height %></td>
        <td><%= link_to 'Show', user_info %></td>
        <td><%= link_to 'Edit', edit_user_info_path(user_info) %></td>
        <%# <><%= link_to 'Destroy', user_info, method: :delete, data: { confirm: 'Are you sure?' } %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'New User Info', new_user_info_path %>
