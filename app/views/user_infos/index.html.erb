<p id="notice"><%= notice %></p>

<%# <h1>Your Profile</h1> %>

<div class="profile">
  <%# <h5>(insert profile image and profile image upload feature)</h5> %>
  
  <h5><%#=@current_user_info %></h5>
  <%#= @current_user_info%>
  <%# <h5>D.O.B.</h5>
  <h5>Gender</h5>
  <h5>Weight</h5>
  <h5>Height</h5> %>

</div>
<% @user_infos.each do |user_info| %>
      
      <div class="profile">
          <h2><%= user_info.name %></h2>
        </div>
      <div class="users-table">
        
        <div class="profile profile-img">
          <%= image_tag(user_info.picture, class:"profile-img") if user_info.picture.attached?%>
        </div>
        <div class="profile">
          <h6>Your date of birth:</h6>
          <h5><%= user_info.date_of_birth %></h5>
        </div>
        <%# <div class="profile">
          <h3>Your gender:</h3>
          <%= user_info.gender %>
        <%# </div> %>
        <div class="profile">
          <h6>Your current weight (in kgs):</h6>
          <h5><%= user_info.weight %></h5>
        </div>
        <div class="profile">
          <h6>Your height:</h6>
          <h5><%= user_info.height %></h5>
        </div>
        <%= link_to 'Show', user_info %>
        <%= link_to 'Edit', edit_user_info_path(user_info) %>
        <%# <div><%= link_to 'Destroy', user_info, method: :delete, data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>



<%# <table>
  <thead class="show-users-head">
    <tr>
      <th>Name</th>
      <th>Date of birth</th>
      <th>Gender</th>
      <th>Weight</th>
      <th>Height</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody class="show-users-body">
    
  </tbody>
</table>

<br> %>

<%#= link_to 'New User Info', new_user_info_path %>
